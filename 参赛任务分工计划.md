# 参赛任务分工计划
## 第二十八届中国机器人及人工智能大赛 · 人工智能创新赛

> 编制日期：2026年5月7日（最新更新：5月12日 18:00）
> 执行周期：5/8（周五）→ 5/19（周二）
> 技术优化 5/8–5/14 · 文档工作 5/15–5/19 · **CANN验证 5/12–5/15（新增）**

---

## 零、当前项目文件清单（v2）

```
thruster_project/
├── v1_baseline/                    # v1基线（保留不动，只读）
│
├── data_pipeline_v2.py             # [M] 17维预处理 + 双输出标签（✅ 完成）
├── model_train_v2.py               # [M] Attention-LSTM训练（✅ 完成）
├── model_test_v2.py                # [M] 测试评估 + 接口npy导出（✅ 完成）
├── analyze_ssf.py                  # [M] SSF诊断分析（✅ 完成）
├── model_shap_v2.py                # [M] 17维SHAP + Permutation（✅ 完成）
├── inference_utils.py              # [M] 推理封装 + MC Dropout（✅ 完成）
│
├── model_anomaly_eval.py           # [Z] 异常检测定量评估（📅 进行中）
├── model_onnx_export.py            # [Z] ONNX导出+INT8量化（📅 进行中）
├── app_v2.py                       # [Z] Streamlit大屏（📅 5/13-14）
│
├── outputs/models/v2/              # 模型权重（✅ 已推送）
├── outputs/predictions/v2/         # 接口npy + SHAP npy（✅ 已推送）
├── outputs/figures/v2/             # 图表（✅ 已推送）
│
├── .gitignore
├── requirements.txt
└── README.md
```

> **v1基线归档在 `v1_baseline/` 目录，只读不改。所有v2工作独立文件。**

---

## 零·五、文件归属与接口约定

### 文件归属

| 文件 | 负责人 | 说明 |
|------|--------|------|
| `data_pipeline_v2.py` | **M** | 17维预处理，所有v2脚本共同import |
| `model_train_v2.py` | **M** | 17维+双输出Attention-LSTM训练 |
| `model_test_v2.py` | **M** | 测试评估 + 接口文件导出 |
| `model_shap_v2.py` | **M** | 17维SHAP归因 + Permutation交叉验证 |
| `inference_utils.py` | **M** | 推理封装 + MC Dropout |
| `model_anomaly_eval.py` | **Z** | 真实标签定量评估 |
| `model_onnx_export.py` | **Z** | ONNX导出 + INT8量化压缩 |
| `app_v2.py` | **Z主导UI，M联调后端** | 新版Streamlit大屏 |
| **以上文件在CANN迁移中S仅修改设备行（cuda→npu），不改逻辑** | **S** | |

### Git分支策略

```
main          ← 稳定版本，5/14集成节点合并
  ├── dev-m   ← M（模型层：pipeline → 训练 → SHAP → 推理封装）
  ├── dev-z   ← Z（评估部署层：异常检测 → ONNX → UI）
  └── dev-s   ← S（CANN迁移层：代码迁移 → NPU重跑 → 对比报告）
```

**操作规范：**
- 每人只在自己的分支上提交，**禁止直接push到main**
- 5/14晚由M执行dev-m + dev-z + dev-s merge到main
- 每次提交前先 `git pull origin 自己的分支`
- commit message格式：`[M/Z/S] 简短描述`

---

## 一、M（队长，技术主导）— 已完成 ✅

### M的已完成任务清单（5/8 - 5/12）

| 日期 | 文件 | 成果 |
|:----:|------|------|
| 5/8 | `data_pipeline_v2.py` | 17维特征pipeline（ton+vl+pressure+3老化因子+11 one-hot），11种mode验证通过 |
| 5/9 | `model_train_v2.py` | Attention-LSTM训练，5轮实验迭代，best val_loss=0.000257 |
| 5/10 | `model_test_v2.py` | 测试评估：thrust RMSE=0.0832N，mfr RMSE=49.1mg/s，v1对比-45.4%，按mode分项表 |
| 5/10 | 接口npy文件 | predictions_dual.npy [1344,200,2] + targets + anomaly_labels + meta_info |
| 5/11-12 | `model_shap_v2.py` | GradientExplainer，thrust/mfr/Isp各17维SHAP，v1 vs v2对比，Permutation交叉验证，11张图 |
| 5/12 | `inference_utils.py` | predict + MC Dropout不确定性 + 残差检测 + 健康报告，已验证 |

### 最终模型指标（锁定v2.0）

| 指标 | v1 | v2.0 | 提升 |
|------|:--:|:----:|:----:|
| Thrust RMSE | 0.1247 N | **0.0832 N** | **-33.3%** |
| MFR RMSE | 无 | **49.1 mg/s** | 全新能力 |
| v1 vs v2 同窗对比 | 0.1247 N | **0.0681 N** | **-45.4%** |
| best val_loss | — | **0.000257** | — |

### SHAP关键发现

| 排名 | Thrust | MFR | Isp |
|:----:|:-------|:----|:----|
| 1 | **cumulated_throughput** | cumulated_throughput | cumulated_throughput |
| 2 | ton | ton | cumulated_on_time |
| 3 | cumulated_on_time | cumulated_on_time | **vl** |

三大老化因子合计占 thrust SHAP 的 **49%**（v1仅2%），Permutation交叉验证排名一致。

### M的待完成任务

#### 5/13（周三）· Streamlit后端对接（与Z联调）

- [ ] 与Z同步`app_v2.py`的当前进展，确认UI组件与`inference_utils.py`的调用接口
- [ ] 将`inference_utils.py`接入`app_v2.py`，确保推理pipeline跑通
- [ ] 联调：三联监控面板（thrust / mfr / Isp）数据是否正确显示
- [ ] 测试"一键生成健康报告"功能的后端逻辑

#### 5/14（周四）· 集成测试 + merge

- [ ] 上午：运行完整Demo，检查所有功能模块正常
- [ ] **下午：M执行merge：`dev-m` + `dev-z` + `dev-s` → `main`**
- [ ] 解决merge冲突
- [ ] 技术成果汇总：整理所有关键图表

---

## 二、Z（队友，评估部署+UI）— 进行中

#### 5/8（周五）· 环境准备 + 文献检索

- [ ] 拉取`dev-z`分支，运行`app.py`验证环境
- [ ] 阅读STFT数据集文档，31种anomaly_code含义
- [ ] 查新报告文献检索（知网 + IEEE + WoS）
  - 检索词：`monopropellant thruster anomaly detection deep learning` / `spacecraft thruster digital twin LSTM` / `SHAP attribution time series PHM` / `dual output regression time series neural network`

#### 5/9-10 · 异常检测评估模块

- [ ] `model_anomaly_eval.py`：load_npy → 残差计算 → P/R/F1/IoU → 混淆矩阵 → 阈值敏感性分析
- [ ] 图表：`05_anomaly_eval_metrics.png`、`06_anomaly_confusion_matrix.png`

#### 5/11 · 接口联调 + ONNX

- [ ] 用真实npy文件替换mock数据，完整运行`model_anomaly_eval.py`
- [ ] 新建 `model_onnx_export.py`：ONNX导出 → FP32/FP16/INT8三档对比

#### 5/12 · ONNX量化

- [ ] 三档量化对比表（模型大小+推理延迟+精度损失）
- [ ] Jetson Nano理论算力估算
- [ ] 图表：`07_onnx_quantization_table.png`

#### 5/13 · Streamlit UI主导开发

- [ ] 新建 `app_v2.py`：深色主题 / 三联面板 / 残差告警 / 健康报告按钮
- [ ] 留好`inference_utils.py`调用接口

#### 5/14 · 联调

- [ ] 配合M完成后端接入联调，参与merge

---

## 三、S（新队员，昇腾CANN国产化算力验证）🔥 新增

> 5/12 加入。独立负责华为云昇腾CANN全流程验证，与CUDA结果形成**国产化算力对比论证**，构成项目核心创新点之一。

### 为什么做（答辩价值）

1. CUDA闭源，中国航天军工须用国产算力 → **"国产化替代"是核心创新点**
2. CUDA已验证模型有效性（thrust RMSE=0.0832N）
3. CANN上重跑验证**算法在国产架构上同样可运行、结果一致**
4. 答辩时可展示CUDA vs CANN对比表 → 证明工程落地能力

### 逐日任务清单

#### 5/12（周二）下午~晚 · 环境搭建

- [ ] 登录华为云官网，手机号注册+实名认证
- [ ] 领取200元昇腾无门槛代金券（控制台→费用中心→代金券管理）
- [ ] 控制台搜索「弹性云服务器ECS」→创建密钥对`ascend-key`
- [ ] 购买弹性云服务器：
  - 计费模式：**按需计费** · 区域：**华北-北京四** · CPU：**鲲鹏计算**
  - 规格：**ai2.xlarge.1**（32GB，8元/h，主力训练）
  - 镜像：**公共镜像 → 人工智能平台 → CANN 9.0 + PyTorch 2.4.0**
  - 系统盘40GB · 公网IP：现在购买
- [ ] SSH连接服务器：`ssh root@服务器IP -i ascend-key.pem`
- [ ] 验证环境：`npu-smi info` + `python3 -c "import torch; import torch_npu; print(torch.npu.is_available())"`
- [ ] 安装依赖：`pip3 install pandas numpy matplotlib scikit-learn streamlit tqdm shap`

#### 5/13（周三）全天 · v1验证 + v2迁移准备

- [ ] GitHub克隆项目：`git clone https://github.com/... && git checkout -b dev-s`
- [ ] 修改`v1_baseline/model_train.py`：`cuda`→`npu`（设备行1处）
- [ ] 上传数据到服务器：本地执行 `scp -r data/ root@IP:/root/thruster_project/`
- [ ] 跑通v1训练（~15-30min），确认环境正常
- [ ] 修改`model_train_v2.py`：头部加`import torch_npu`，`cuda`→`npu`
- [ ] 修改`model_test_v2.py`：同上
- [ ] `git add` + `git commit -m "[S] CANN: cuda→npu设备配置"` + `git push`

#### 5/14（周四）全天 🔥 v2核心验证

- [ ] **早上开始v2训练**（~2h）：`python3 model_train_v2.py`
  - 保存训练日志，记录每epoch loss（后续画对比图用）
  - 期望：best val_loss ≈ 0.000257（与CUDA一致）
- [ ] 训练完成后确认模型保存：`ls outputs/models/v2/`
- [ ] 跑v2测试：`python3 model_test_v2.py`
  - 记录thrust RMSE / MFR RMSE / MAE
  - 期望：thrust RMSE ≈ 0.0832N（与CUDA一致）
- [ ] 记录训练总时长、推理速度
- [ ] `git add` + commit + push（含outputs）

#### 5/15（周五）上午 · SHAP验证 + 下午 · 对比报告

- [ ] 跑SHAP：`python3 model_shap_v2.py`（CPU模式，不改代码）
  - 记录Thrust/MFR/Isp各Top-3排名
  - 与CUDA结果对比，标记是否一致
- [ ] 整理CUDA vs CANN对比表（见下方格式）
- [ ] 绘制loss收敛对比图（参考指南中的脚本）
- [ ] 对比报告交付M，写入研究报告

#### CUDA vs CANN对比表（S填写）

| 对比项 | CUDA（NVIDIA） | CANN（华为昇腾） | 是否一致 |
|:-------|:--------------:|:--------------:|:--------:|
| v2 Training Time | ~40 min | ? min | — |
| v2 Thrust RMSE | 0.0832 N | ? N | ? |
| v2 MFR RMSE | 49.1 mg/s | ? mg/s | ? |
| v2 Inference Speed | ? ms/sample | ? ms/sample | ? |
| SHAP Thrust Top-1 | cumulated_throughput | ? | ? |
| SHAP Top-3 match | — | — | ? ✅/❌ |

### 对应文件与改动

分支：`dev-s`，仅修改设备配置行，不修改任何模型逻辑。

| 文件 | 改动 |
|------|------|
| `model_train_v2.py` | 头部加 `import torch_npu`；`cuda`→`npu`，`torch.cuda`→`torch.npu` |
| `model_test_v2.py` | 同上 |
| `model_shap_v2.py` | SHAP强制CPU模式，无需改动 |

### CANN SHAP说明

SHAP在CPU运行，CANN下不用改代码。目的是**数值一致性验证**：
- CANN SHAP排名与CUDA一致 → 证明算法在国产架构上数值可靠
- 有偏差 → 记录偏差范围
- 答辩结论："CUDA与CANN的SHAP排名完全一致，国产化部署精度可靠"

### 关键输出

| 输出 | 用途 |
|:-----|:------|
| CUDA vs CANN loss收敛对比图 | 研究报告 + 答辩 |
| 测试指标对比表（RMSE/MAE） | 研究报告 |
| SHAP排名一致性表 | 国产化验证论证 |
| 训练/推理速度对比 | 答辩亮点 |

---

## 四、文档工作任务（5/15–5/19）

### 5/15（周五）

**M**：
- 研究报告·章节③：背景和国内外研究现状（含文献对比表）
- 研究报告·章节④：研究内容与技术路线（含系统架构图）
- 研究报告·章节⑤：项目创新点（含国产化算力验证）

**Z**：
- 查新报告全文撰写（送学院盖章）
- 报名表填写

**S**：
- CUDA vs CANN对比报告定稿，将结果交付M写入研究报告

### 5/16（周六）

**M**：
- 研究报告·章节⑦：存在的问题与改进方向（含CANN对比）
- 研究报告全文统稿、排版、转PDF
- **下午送学院盖章（报名表E栏）**

**Z**：
- 研究报告·章节①②：题目+摘要
- 研究报告·章节⑥：应用前景与社会价值
- 参考文献整理（≥10篇）
- 提交软著申请

### 5/17（周日）

**M**：答辩讲稿（10分钟）+ 图表整理 + 取回盖章件
**Z**：答辩PPT制作 + Demo操作视频录制

### 5/18（周一）

全材料QC检查（报名表/研究报告/查新报告/告知书/缴费凭证/软著申请号）
**Z**：800元缴费 + PPT定稿

### 5/19（周二）· 提交日

M主导，Z协助：上午10:00前系统提交所有材料

---

## 五、5个核心创新点（更新：含国产化验证）

1. **多源异质动态特征融合**：6维退化因子（时间/通量/脉冲/工况/阀电平）联合建模，覆盖催化床三种退化机制
2. **多目标耦合预测架构**：双输出Attention-LSTM联合预测推力+质量流量，推导比冲Isp
3. **基于真实标签的可量化异常检测**：270个真实异常标签 → P/R/F1/IoU四指标
4. **个性化迁移学习应对制造公差**：极小学习率微调，v1预测误差降低27.97%
5. **🔥 国产化算力适配验证（新增）**：在华为昇腾CANN上重跑全流程，CUDA vs CANN对比证明算法在国产架构上精度一致，具备军工落地条件

---

## 六、任务总览甘特图（含CANN）

```
        5/8  5/9  5/10 5/11 5/12 5/13 5/14 | 5/15 5/16 5/17 5/18 5/19
        周五 周六  周日  周一  周二  周三  周四 | 周五  周六  周日  周一  周二
─────────────────────────────────────────────────────────────────────────────────
M       pipe  训练  测试  接口  SHAP  大屏  集成 | 报告   报告  答辩  QC   提交
                              归因  联调  merge | ③④⑤  ⑥⑦  讲稿
─────────────────────────────────────────────────────────────────────────────────
Z       文献  异常  评估  接口  ONNX  UI   联调 | 查新  摘要  PPT   缴费  协助
                                           +并 | 报告  前景  制作  盖章
─────────────────────────────────────────────────────────────────────────────────
S                  【5/12 18:00 加入团队】
                    环境   v1    v2训练  v2测试  SHAP  对比
                    搭建   验证   🔥全量         验证   报告
                        （5/12 晚~5/13）→（5/14）→（5/15）→交付M
─────────────────────────────────────────────────────────────────────────────────
盖章                                                  查新↑      报名↑
缴费                                                              缴费↑
提交                                                                  系统↑
```

---

## 七、文件隔离（免冲突规则）

| 文件 | 专属负责人 | 其他人 |
|------|:---------:|:------:|
| `data_pipeline_v2.py` | **M** | Z/S不碰 |
| `model_train_v2.py` | **M** | Z/S仅CANN改设备行 |
| `model_test_v2.py` | **M** | Z/S仅CANN改设备行 |
| `model_shap_v2.py` | **M** | Z/S仅CANN改设备行（实际不需改）|
| `inference_utils.py` | **M** | Z只读调用 |
| `model_anomaly_eval.py` | **Z** | M/S不碰 |
| `model_onnx_export.py` | **Z** | M/S不碰 |
| `app_v2.py` | **Z主导UI** | 5/13前M不碰，5/14联调时M改后端需当面沟通 |

---

## 八、Git操作指南

### M的git指令

```bash
# 每天开始
git pull origin dev-m

# 提交
git add 具体文件名
git commit -m "[M] 完成XXX"
git push origin dev-m

# 查看Z/S进度
git fetch origin dev-z dev-s
git log origin/dev-z --oneline -5
git log origin/dev-s --oneline -5
```

### Z的git指令

```bash
git pull origin dev-z
git add model_anomaly_eval.py model_onnx_export.py app_v2.py
git commit -m "[Z] 完成XXX"
git push origin dev-z

# 拉取M的接口文件
git fetch origin dev-m
git checkout origin/dev-m -- outputs/predictions/v2/
```

### S的git指令

```bash
git pull origin dev-s
# 改完设备行后提交
git add model_train_v2.py model_test_v2.py
git commit -m "[S] CANN: cuda→npu设备配置"
git push origin dev-s

# 拉取M最新代码
git pull origin dev-m
```

### 5/14集成操作（M执行）

```bash
git checkout main
git pull origin main
git merge dev-m
git merge dev-z
git merge dev-s   # S的CANN改动仅设备行，无冲突
git push origin main
```

---

## 九、关键风险提示

| 风险 | 概率 | 影响 | 应对 |
|:-----|:----:|:----:|:-----|
| CANN环境配置异常（鲲鹏+昇腾版本不匹配） | 中 | 高 | 严格使用官方CANN 9.0 + PyTorch 2.4.0镜像 |
| CANN代金券不够用 | 低 | 中 | 200元足够25h训练，关机节省 |
| CANN训练速度比CUDA慢 | 高 | 低 | 速度不是目标，**结果一致性**才是 |
| Z的异常评估进度滞后 | 中 | 中 | M保留5/14兜底时间 |
| 学院盖章周期>2天 | 中 | 高 | 5/15、5/16分别送审 |
| 报名系统5/19拥挤 | 中 | 中 | 5/19上午10点前完成 |
