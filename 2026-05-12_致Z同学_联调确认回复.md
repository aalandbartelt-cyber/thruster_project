# 致Z同学：5/12 回复 —— 联调确认 + 数据说明

> M 回复，2026-05-12

---

## 一、你已完成的工作 👍

看到你的进度，效率很高，三件事都做得很好：

| 你的模块 | 状态 | 我的评价 |
|:---------|:----:|:---------|
| `model_anomaly_eval.py` | ✅ 完成 | 召回率 0.008→0.992（融合mfr）这个发现很好，研究报告的价值点 |
| `model_onnx_export.py` | ✅ 完成 | FP32 42.5ms → INT8 3.9ms，10倍加速，答辩亮点 |
| `app_v2.py` | ✅ 完成 + 联调测试 | 与 inference_utils.py 全部函数集成测试通过，效率很高 |

---

## 二、CSV 测试数据（已解决 ✅）

`sample_data/` 目录已推送到 GitHub，包含 6 个代表性测试 CSV 文件 + 对应的 metadata 子集：

| 文件 | mode | 特征 |
|:-----|:-----|:------|
| `01269_001_SN13_24bars_ssf.csv` | ssf | 稳态，最常见工况 |
| `01275_007_SN13_24bars_health_check.csv` | health_check | 低压稳态，模型最优 |
| `01281_013_SN13_24bars_ramp1.csv` | ramp1 | 斜坡增推 |
| `01285_017_SN13_24bars_onmod.csv` | onmod | 脉冲开启，模型最佳 |
| `01286_018_SN13_24bars_offmod.csv` | offmod | 脉冲关断，瞬态最难 |
| `01289_021_SN13_24bars_random_mixed.csv` | random_mixed | 随机混合模式 |

**使用方式：**

```python
import pandas as pd
from data_pipeline_v2 import load_sequence

# 读 metadata 子集（只有这 6 个文件）
metadata = pd.read_csv("sample_data/metadata_subset.csv")
metadata['filename'] = metadata['filename'].str.strip()

# 选一个文件
fname = "01285_017_SN13_24bars_onmod.csv"
meta_row = metadata[metadata['filename'] == fname].iloc[0]

# 加载 17 维特征
x, y, labels = load_sequence(f"sample_data/{fname}", meta_row, seq_len=200)

# 传给模型推理
import torch
x_tensor = torch.from_numpy(x).float().unsqueeze(0)
thrust_pred, mfr_pred, isp = predict(model, x_tensor)
```

> `sample_data/` 目录已直接提交到 GitHub，`git pull origin dev-m` 即可获取。

---

## 三、关于 `load_sequence()` 的调用方式

你的理解完全正确。`meta_row` 就是直接从 `metadata.csv` 中读取对应行的 **pandas Series**：

```python
import pandas as pd
from data_pipeline_v2 import load_sequence

# 正确用法 ✅
metadata = pd.read_csv("data/metadata.csv")
metadata['filename'] = metadata['filename'].str.strip()

# 找到你要的那个文件
row = metadata[metadata['filename'] == '你要的文件名.csv'].iloc[0]

# 直接传这个 Series 作为 meta_row
x, y, labels = load_sequence("data/dataset/dataset/test/你要的文件名.csv", row, seq_len=200)
# x:       [200, 17]  输入特征
# y:       [200, 2]   标签（已归一化）
# labels:  [200]      anomaly_code

# 然后转 Tensor 传给 predict：
import torch
x_tensor = torch.from_numpy(x).float().unsqueeze(0)  # [1, 200, 17]
thrust, mfr, isp = predict(model, x_tensor)
```

`load_sequence()` 内部会用 `meta_row['test_pressure']`、`meta_row['cumulated_throughput']` 等字段来构建 17 维特征，所以 **metadata 的那一行必须包含完整的字段**。

---

## 四、界面细节回复

| 你的问题 | 我的建议 |
|:---------|:---------|
| 健康报告手动点击 vs 自动刷新 | **手动点击即可**，答辩时可以表演"一键生成"，更有仪式感 |
| 侧边栏阈值滑动条 | 保留，默认 0.6N，答辩时可现场调节展示灵敏度 |
| 文件上传组件 | 留好接口就行，明天联调时确定最终样式 |

---

## 五、联调计划（5/13 上午）

**时间**：5/13 上午（具体时间你定）

**我的准备**：
- 已准备好 `inference_utils.py`（predict + MC Dropout + 残差 + 健康报告）
- 接口 npy 文件已就绪

**联调流程**：

```
① 确认 app_v2.py 数据源 → 用 npy 数据展示
② 跑通三联面板（thrust / mfr / Isp）
③ 确认残差告警红色高亮正常
④ 测试一键健康报告 → 文字输出
⑤ 录制 Demo 视频（备答辩用）
⑥ 如果有时间：讨论研究报告分工
```

---

## 六、Git 说明

你的 `dev-z` 分支已经推送，我会在明天联调前拉取看一下你的代码。

如果有任何接口调整需要，明天当面改，速度最快。
