# 优化方向：个体推进器微调（Per-Thruster Fine-Tuning）

> 编写日期：2026-05-13（最新更新：5/14）
> 前置工作：v1→v2 全局模型已完成，v1 已有 `model_finetune.py` 可复用经验  
> 当前状态：`model_finetune_v2.py` 已实现并推送到 dev-m
> 目标：解决"一个模型适配所有推进器导致误差较大"的问题

---

## 一、问题背景

当前 v2.0 模型使用全部 908 个正常文件（SN01-SN12）训练，服务所有推进器（SN13-SN24 测试集）。

**现有问题：**
- 全局模型 thrust RMSE = 0.0832N，但按 test_mode 分项后 ssf 高达 0.1390N
- 不同 SN 之间推力水平差可达 2 倍（SN13 mean=0.65N vs SN18 mean=0.97N），已在 v2.1 实验中发现
- 制造公差导致同一工况下不同推进器的响应特性不同

**差异化根源：**
- 推进器制造公差：催化床填充量、喷嘴尺寸、阀门响应时间差异
- 使用历史差异：cumulated_on_time / throughput / pulses 三个老化因子无法完全刻画个体差异

### 1.1 SN 编号的含义：同一型号下的不同生产个体

**SN01-SN24 是同一款单组元推进器（hydrazine monopropellant thruster）的 24 台不同生产个体，不是 24 种不同类型。** 类似于同一款手机的不同台次或同一车型的不同车辆。

所有 SN 共享相同的设计图纸和物理原理：
- 相同的催化床结构
- 相同的喷嘴口径
- 相同的阀门类型
- 相同的推进剂（肼）

但制造公差客观存在：
- 催化床填充密度差异：±3-5%
- 喷嘴加工精度差异：±1-2%
- 阀门响应时间差异：±2-3ms

v2.1 实验已经证实了这些公差的影响：**SSF 模式下 SN13 平均推力 0.65N vs SN18 平均推力 0.97N，相差约 2 倍。**

### 1.2 为什么用 SN01-12 训练、SN13-24 测试？

STFT 数据集的 24 个 SN 编号本身只是合成数据的标识符，不区分"地面试验件"和"飞行件"。我们沿用了 v1 的数据划分方式：**SN01-12 做训练集、SN13-24 做测试集**，这是标准的机器学习数据拆分。

这个拆分方式同时对应了一个合理的工程叙事：用一部分推进器的数据训练模型，去预测另一部分推进器的行为。在真实航天场景中，这种"用地面试验数据训练、用在轨飞行数据监控"的路线是标准做法。

### 1.3 微调的数据来源——在实验中的实际做法

每台推进器（SN13-SN24）在测试集中各有 **约 100 个正常文件**。微调是在实验框架下验证方案可行性的对比实验：

```
以 SN13 为例（103 个正常文件）：
  ├── 微调集（5 个文件）    ← 从 SN13 自己的文件中取出少量，在全局模型基础上继续训练
  └── 测试集（98 个文件）   ← SN13 剩下的文件，用来对比"微调前 vs 微调后"的预测精度
```

| 数据拆分 | 文件数 | 说明 |
|:---------|:------:|:------|
| 全局训练集（SN01-12） | 908 | 训练基础模型 |
| **微调集（目标 SN）** | **5 / SN** | **从该 SN 的测试文件中取 5 个，用于微调** |
| 测试集（目标 SN） | ~100 / SN | 剩余文件，评估微调效果 |

**为什么要取 5 个？** 这不是固定值——我们会做多组对比来确定最少需要多少数据：

| 微调用文件数 | 实验目的 |
|:-----------:|:---------|
| 1 | 极限少量数据下微调是否有效 |
| **5（baseline）** | **合理少量场景，主方案** |
| 10 | 数据增加后改善是否继续 |
| 50（≈一半） | 微调技术本身的上限 |

最终在研究报告中可以写："**仅用 5 条点火序列微调该推进器的专用模型，RMSE 降低 X%**"——这是用实验设计支撑的结论。至于真实航天场景中需要多少次校准点火，需要在取得 803 所的真实数据后确定。

### 1.4 全局模型 vs 微调模型的关系

| 维度 | 全局模型（v2.0） | 微调模型（v2.x） |
|:-----|:----------------|:----------------|
| 训练数据 | SN01-SN12，908 个文件 | 目标 SN 的 n 个文件（通常 10-50 个） |
| 能力 | 学会推进器的**通用物理规律** | 在通用规律基础上**适配个体偏差** |
| 精度 | thrust RMSE = 0.0832N | 预期 0.05-0.06N（-30%） |
| 置信度 | MC Dropout CV 较大（65/100） | 预期提升至 85+/100 |
| 泛化性 | 对所有 SN 有效 | 仅对目标 SN 有效 |

**微调不是否定全局模型，而是在全局模型的基础上做个性化适配。** 全局模型提供基础预测能力，微调修正个体公差导致的系统偏差。

---

## 二、v1 微调经验回顾

v1 的 `model_finetune.py` 方案：

| 项目 | 值 |
|:-----|:----|
| 预训练模型 | v1 SimpleLSTM（3 维单输出） |
| 微调目标 | SN13（单台推进器） |
| 学习率 | **1e-4**（远低于初始训练的 1e-3） |
| 数据量 | 该 SN 的全部正常文件 |
| 冻结策略 | 全部权重参与微调（无冻结层） |
| 效果 | RMSE 降低 **27.97%** |

---

## 三、v2 微调实现（`model_finetune_v2.py`，已推送到 dev-m）

### 3.1 文件信息

| 项目 | 值 |
|:-----|:-----|
| 文件名 | `model_finetune_v2.py` |
| 分支 | `dev-m` |
| 基础模型 | `outputs/models/v2/dual_output_lstm_v2.pth`（Attention-LSTM） |
| 输出路径 | `outputs/models/v2/dual_output_lstm_v2_sn{sn}_{strategy}.pth` |
| 评估方式 | 滑动窗口全序列覆盖（stride=200），非仅首 200 步 |
| 严格分离 | 微调/验证(3文件)/测试(剩余) 三集不重叠 |

### 3.2 微调策略

```
默认策略：冻结 LSTM + Attention，仅微调 FC head + LN
  - FC(256→128) + FC(128→2) 全部参数
  - LayerNorm 的 γ/β

学习率：2e-4（单学习率，干净可解释）
优化器：Adam + ReduceLROnPlateau + 梯度裁剪 max_norm=1.0
早停：patience=8，保存验证集最佳模型

对照模式（--unfreeze_all）：全网络微调（v1 风格）
```

### 3.3 评估指标

| 指标 | 计算方式 |
|:-----|:---------|
| Thrust RMSE | 滑动窗口全序列预测 vs 真实值 × THRUST_SCALE |
| MFR RMSE | 同上 × MFR_MAX |
| Isp RMSE | `thrust / (mfr × 1e-6 × G0)` 预测 vs 真实 |
| 改善幅度 | `(baseline - finetuned) / baseline × 100%` |
| 统计检验 | 单样本 t 检验（H₀: 改善=0, 12 SN） |
| 灾难性遗忘 | 微调后在其他 11 个 SN 上各测 forget_n_files 文件 |

### 3.4 用法

```bash
# 全部 SN13-24（默认 n_finetune=12，冻结 LSTM）
python model_finetune_v2.py

# 单台推进器
python model_finetune_v2.py --sn 13

# 全网络微调对照（v1 风格）
python model_finetune_v2.py --unfreeze_all

# 扫描微调数据量（画边际收益曲线）
python model_finetune_v2.py --scan_n 3,6,12,24

# 控制遗忘检查规模
python model_finetune_v2.py --forget_n_files 3
```

### 3.5 代码质量修复记录（2026-05-14）

| 级别 | 问题 | 修复 |
|:----:|:-----|:------|
| Bug | `freeze_lstm or True` 永真表达式 | 改为 `--no_freeze_lstm`，反向语义 |
| Bug | Isp `1e-6` 与 `1e-3` 全项目不一致 | 统一 `1e-6`（物理正确） |
| Bug | 评估只取前 200 步 | 滑动窗口全序列覆盖 |
| Bug | 验证/测试集重叠 | 严格三分离 |
| Bug | `eval_many` 空列表保护缺失 | 加 `if not t_list: return 0,0,0` |
| 设计 | 遗忘检查重复计算 11 遍 | 预计算 `global_perf_by_sn` 字典 |
| 设计 | 模型权重文件互相覆盖 | 文件名加策略标识 `_freeze_lstm` / `_unfreeze` |
| 设计 | 验证集只用 1 个文件 | 3 个文件平均 loss |
| 设计 | 无随机种子 fix | `torch.manual_seed(42)` + `cuda.manual_seed_all(42)` |
| 加分 | 扫参无边际收益曲线 | `errorbar` 汇总图 |
| 加分 | 遗忘判定阈值 5% 太宽松 | 三档判定（2%/10%分界） |
| 加分 | `model_test_v2.py` Isp 缺 `1e-6` | 已修 |
| 加分 | 扫参曲线缺 `os.makedirs` | 已加 |
| 加分 | `--forget_n_files` 参数未生效 | 已绑定 |

### 3.6 输出产物

| 输出 | 说明 |
|:-----|:------|
| `outputs/models/v2/dual_output_lstm_v2_sn13_freeze_lstm.pth` | SN13 微调模型（冻结 LSTM） |
| `outputs/models/v2/dual_output_lstm_v2_sn13_unfreeze.pth` | SN13 微调模型（全网络） |
| `outputs/figures/v2/finetune_improvement_n12.png` | n_finetune=12 时各 SN 改善柱状图 |
| `outputs/figures/v2/finetune_scan_curve.png` | 边际收益曲线（扫参模式） |

### 3.7 预期效果

| 推进器 | 全局 RMSE（测试时） | 微调预期 |
|:-------|:------------------:|:--------:|
| SN13（全局模型） | 0.1337 N | 首次测试 +17.9% thr / +23.7% mfr（冻结策略，n=5） |
| 全部 SN13-24 平均 | 待跑 | 预期 >15% |

---

## 四、App 页面整合（待实施）

微调完成后，app_v2.py 按 SN 加载对应模型：

```python
model_paths = {
    13: "outputs/models/v2/dual_output_lstm_v2_sn13_freeze_lstm.pth",
    14: "outputs/models/v2/dual_output_lstm_v2_sn14_freeze_lstm.pth",
}
sn = meta_row['sn']
path = model_paths.get(sn, "outputs/models/v2/dual_output_lstm_v2.pth")
model = load_dual_model(path)
```

UI 修改：
- 顶栏状态显示 `MODEL: GLOBAL` 或 `MODEL: SN13-TUNED`
- 遥测卡片增加微调标识标签

---

## 五、实施优先级

| 优先级 | 任务 | 预计时间 | 状态 |
|:------:|:-----|:--------:|:----:|
| P0 | 全局模型 | 5/13 | ✅ 完成 |
| P1 | **微调脚本开发** | 5/14 | ✅ 已实现（含扫参/遗忘检查/统计检验） |
| P2 | 微调全部 12 台 | 待跑 | 📅 |
| P3 | App 加载逻辑改造 | 0.5 天 | ⬜ 待完成微调后 |
| P4 | 对比实验（研究报告） | 0.5 天 | ⬜ |

---

## 六、核心意义：我们不是在自导自演

### 6.1 关于合成数据的坦诚

本项目使用 Kaggle 上的 STFT 合成数据集（Spacecraft Thruster Firing Tests Dataset）。我们无法获得 803 所的真实航天数据，所有关于"地面试验/在轨飞行"的叙事均为比赛框架下的工程推演。

**但这不意味着我们在自导自演。** 以下成果不依赖数据来源，反映的是真实的工程能力。

### 6.2 工程落地路线：导师团队与 803 所合作关系

本项目指导教师 **贺伟国副教授** 与 **中国航天科技集团第八研究院第 803 研究所** 有深度合作关系。这意味着本项目不是纸上谈兵——具备获取真实推进器测试数据、开展工程验证的通道。

| 合作阶段 | 内容 | 时间预期 |
|:---------|:-----|:---------|
| 当前阶段（比赛） | 使用合成数据验证算法 pipeline 的完整性和有效性 | 2026.05 |
| 下一阶段 | 通过导师团队对接 803 所，获取真实单组元推进器点火测试数据 | 比赛后 |
| 工程落地 | 在真实数据上重新训练、标定阈值、部署至推理系统 | 803 所协调后 |

### 6.3 拿到真实数据后如何落地

如果取得 803 所的真实推进器数据，以下是我们当前工作与真实工程落地的差距分析：

| 模块 | 当前（合成数据） | 真实工程（需改动） | 改动量 |
|:-----|:----------------|:------------------|:------:|
| **数据接口** | `data_pipeline_v2.py` 读取 CSV 格式 | 可能需要适配 803 所的数据格式（可能不是标准 CSV） | **小**——只需修改 `load_sequence()` 的解析层 |
| **特征工程** | 17 维：ton + vl + pressure + 3 老化因子 + 11 one-hot | 803 所数据可能提供更多传感器通道（温度、振动等），特征可扩展 | **小**——架构已支持输入维度可配 |
| **模型架构** | Attention-LSTM，17→2 输出 | 架构不变，输入维度可能变化 | **无** |
| **异常标签** | STFT 数据集的 anomaly_code（31 种） | 803 所可能有自己的异常分类体系 | **中**——需重新映射标签 |
| **健康阈值** | 比赛中使用的阈值（0.25N 等） | 需根据真实数据的噪声水平重新标定 | **中**——需专家参与 |
| **部署硬件** | NVIDIA GPU + 华为昇腾 CANN（已验证） | 803 所可能指定特定硬件平台 | **无**——CANN 已验证，可适配 |
| **SHAP 归因** | 基于合成数据的特征重要性 | 真实数据的 SHAP 结果可能揭示新的物理洞察 | **小**——代码不变，重新跑即可 |
| **置信度校准** | MC Dropout 的变异系数评分 | 需根据真实数据的噪声分布重新校准置信度区间 | **中**——需真实数据标定 |
| **残差阈值** | 比赛中动态阈值 | 需结合 803 所的故障判定标准 | **小**——阈值参数可配 |
| **云端/边缘架构** | 本地 Streamlit 演示 | 可能需要部署到边缘设备或云平台 | **中**——架构不变，部署方式调整 |

**核心结论：** 当前 pipeline 约 **80% 的模块在真实数据下可直接复用**——改动量最大的部分集中在数据接口适配和阈值标定，而非模型本身。这正是本项目的核心竞争力：不是针对某个特定数据集的过拟合方案，而是可迁移的通用 AI 工程框架。

### 6.4 我们真正交付了什么

**完整的 AI 工程闭环：**

```
数据清洗 → 特征工程（17维联合建模）
  → 模型选型（5 轮架构对比）
    → 训练 + 早停 + 可复现性验证
      → 测试（11 种 mode 分项 RMSE）
        → 可解释性（SHAP + Permutation）
          → 不确定性量化（MC Dropout）
            → 跨平台验证（CUDA → CANN 国产化）
              → 部署界面（NASA 风格大屏）
                → 个体微调方案（本文件）
```

这个 pipeline 本身是真实的。换真实数据进来，数据接口一换即可复用。

**所有技术结论经得起推敲：**

| 结论 | 实验证据 |
|:-----|:---------|
| Attention-LSTM 优于纯 LSTM | val_loss 0.000277→0.000252，且 v1 5 架构对比支持 |
| 17 维特征优于 3 维 | RMSE 0.1247→0.0832 N（-33.3%），v1 vs v2 同窗对比 -45.4% |
| 老化因子比控制信号更重要 | SHAP 显示三大老化因子占 49%，ton 仅 32% |
| dropout 在 LSTM 循环连接中不适用 | Round 1/2 实证：dropout=0.1 即破坏时序信号 |
| CANN 与 CUDA 结果一致 | S 验证中——证明算法可部署在国产算力上 |
| 负结果也有价值 | v2.1 SN Embedding 失败 → 发现训练-测试分布偏移问题 |
| 消融实验的价值 | 5 轮实验的因果链本身就是研究叙事 |

这些结论在合成数据上成立，在真实数据上大概率也成立——因为它们反映的是推进器的**物理结构和数据规律**，不是特定数据集的偶然特征。

### 6.5 比赛的评判逻辑

CRAIC 人工智能创新赛评审的是**创新思路 + 技术实现 + 落地可行性**，而非"你有没有真实航天数据"。评委清楚本科生无法取得 803 所数据。

我们展示的完整技术路线意味着：
> "我们理解问题、设计了方案、全部实现并验证了。如取得真实数据，仅需替换数据接口，pipeline 可直接复用。"

这才是基于合成数据参赛的正确姿势——不伪装数据，但用扎实的工程闭环证明技术可行性。

### 6.6 MC Dropout 与"LSTM 不能用 dropout"是否矛盾？

不矛盾。这是两个不同的事情：

| Round 1/2 的结论 | MC Dropout 的做法 |
|:-----------------|:------------------|
| **LSTM 循环连接中**加 dropout → 200 步累积衰减 → 训练崩溃 | **FC 全连接层**加 dropout=0.05，推理时开启多次前向 |
| 影响的是 LSTM 隐状态的时序传递 | 影响的是 FC 层的输出，与 LSTM 无关 |
| 结论：不要在 LSTM 层使用 dropout | 结论：在 FC 层用 dropout 做不确定性量化完全可行 |

我们的模型架构中 LSTM 层无 dropout，仅 `FC(128→2)` 前有 `Dropout(0.05)`。MC Dropout 利用这一层做多次采样，不影响 LSTM 的时序建模能力。

---

## 七、风险

| 风险 | 可能性 | 应对 |
|:-----|:------:|:------|
| 某些 SN 数据量不足 | 低 | 数据增强（加噪声、时间抖动） |
| 微调过拟合（仅几十个文件） | 中 | 早停 patience=5，极小 lr |
| 微调后泛化性下降 | 中 | 保留全局模型作为 fallback |
| 对 test_mode 分项改善不一致 | 中 | 按 mode+SN 交叉分组 | |









